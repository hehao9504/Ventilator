1、显示的参数少，没有列出所要求显示的所有参数。而且显示的参数数量还不恒定。例如设置参数有时候显示5个，有时候3个，而呼吸频率始终未显示。同样监测参数显示数量也很少，仅有3个。
回应：采集数据中没有包含全部数据，以2025-1-27中尾号为3814的JSON为例，
设置参数只有："SET_TRIGGERFLOW",
			"SET_PEEP",
			"SET_PSUPP",
			"SET_SLOPE",
			"SET_OXYGEN",
			"SET_APNEAVENTPINSP",
			"SET_APNEAVENTTINS",
			"SET_VENTMODE",
监控参数只有：	"FIO2",
			"WEIGHTVT",
			"TINS",
			"IE",
			"PMEAN",
			"PPLAT",
			"MVE",
			"MVLEAK",
			"VTE",
			"VTI",
			"RATE",
			"FSPN",
			"IR",
			"ER",
			"RSBI",
			"COMPL",
			"CD",
			"VTESPN",
			"MVSPN",
按照原先的设定，程序显示的没有问题。我们也观察了几组数据（观察结果见“数据解析.txt”文件），不同时间采集的数据存在显著不同，即使是同一类型呼吸机采样的数据也不一样。			

2、弹出框没有最小化按钮，也不能缩放，只有固定大小。
可以缩放（右下角），最小化功能已添加。

3、不能关闭数据，仅能关闭整个程序。


4、右键打开方式，选择该软件，无法打开数据，仅能打开软件。


5、只能从打开的软件中再去打开数据，而且不能记住上次打开的文件位置，每次都要重新寻找定位要打开数据，耗费大量时间。


6、为什么打开一个数据，仅做一处修改，然后保存，文件体积就明显增加，是否增加了很多非必要数据？
已经修改，保存部分修改为使用压缩格式（无缩进和空格），从而生成更紧凑的 JSON 文件，避免因格式化造成额外存储开销。
// 使用紧凑格式保存 JSON 数据，避免额外空格导致体积膨胀
const jsonString = JSON.stringify(modifiedJsonData); 
	
7、打开数据时，可否显示呼吸机品牌？
回应：已添加，可以在左侧显示